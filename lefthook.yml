pre-commit:
  parallel: true
  commands:
    vet:
      run: just vet
    lint:
      run: just lint
    test:
      run: just test
    vuln:
      run: just vuln

commit-msg:
  commands:
    commitlint:
      run: npx --yes commitlint --edit {1}
